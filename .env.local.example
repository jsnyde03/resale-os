# Copy to .env.local and set a real password before serving anything but localhost.
# `npm run serve` refuses to start without this, and refuses fewer than 12 characters.
RESALE_PASSWORD=

# Optional. Defaults to being derived from the password, which is fine for one
# operator on a private tailnet. Set it to rotate sessions without changing the
# password.
RESALE_SESSION_SECRET=
